git is VCS System.
VCS=Version Control System

3 stage in github 

1.modified stage(U,M)
2.staging state(A)(git add .)(A)
3. commit

commit
1.checkout commit 
2.revert commit 
3. reset commit 

git checkout commit-id
git revert commit-id
git reset --hard commit-id

branch

git branch -a 
commit are on the master branch

how to create a new branch
git branch -M branchname

checkout branch
git checkout master
git checkout branch-name

how to direct create a branch and go to branch
git branch -B branch-name
